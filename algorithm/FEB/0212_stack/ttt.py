for i in range(1, N + 1):
    # i는 현재 확인할 전구의 위치를 나타냄.
    # range(1, N + 1)을 사용하여 1번 전구부터 N번 전구까지 순서대로 검사한다.
    # i = 1 → 1번 전구 검사
    # i = 2 → 2번 전구 검사
    # i = N → N번 전구 검사

    if switches[i - 1] == 1:
    # switches 리스트는 전구의 상태를 저장한 리스트.
    # switches[i - 1]은 i번 전구의 상태를 나타냄 (1이면 켜짐, 0이면 꺼짐).
    # 만약 현재 i번 전구가 켜져 있으면, 스위치를 눌러야 한다.
        press_count += 1
        # 스위치를 누른 횟수를 하나 증가시킨다.
        # press_count는 전구를 끄기 위해 스위치를 누른 총 횟수를 저장하는 변수다.
    for k in range(i, N + 1):
        # k는 i번 스위치를 눌렀을 때 영향을 받는 전구의 위치를 의미한다.
        # range(i, N + 1)을 사용하여, i번 전구부터 N번 전구까지 차례대로 확인한다.
        if k % i == 0:
        # i의 배수 위치에 있는 전구만 선택한다.
        # 예를 들어, i = 2라면 2의 배수(2, 4, 6, …)만 선택하여 상태를 반전시킨다.
            switches[k - 1] = 1 - switches[k - 1]
            # 전구 상태를 반전(0 ↔ 1)시키는 코드.
            # switches[5256_binomial coefficient - 1]이 현재 0(꺼짐)이라면 1(켜짐)으로 변경.
            # switches[5256_binomial coefficient - 1]이 현재 1(켜짐)이라면 0(꺼짐)으로 변경.
            # 예제 시뮬레이션
                # 초기 상태: [1, 0, 1, 0] (YNYN)

    # i = 1 (1번 전구가 켜져 있음 → 스위치 누름)

    # 1의 배수 위치(1, 2, 3, 4) 반전
    # 복사
    # 편집
    # [1, 0, 1, 0]  # 초기 상태
    # ↓ (1번 전구 눌림)
    # [0, 1, 0, 1]  # 1의 배수(1,2,3,4) 반전됨
    # i = 2 (2번 전구가 켜져 있음 → 스위치 누름)

    # 2의 배수 위치(2, 4) 반전
    # [0, 1, 0, 1]
    # ↓ (2번 전구 눌림)
    # [0, 0, 0, 0]  # 2의 배수(2,4) 반전됨
    # i = 3 (전구가 꺼져 있음, 패스)
    # i = 4 (전구가 꺼져 있음, 패스)