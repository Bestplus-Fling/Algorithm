import sys
sys.stdin = open('input.txt', 'r')
#####################################

"""
두 명의 일꾼이 있다.
꿀을 채취할 수 있는 벌통의 수 M이 주어질 때
각각의 일꾼은 가로로 연속되도록 N개의 벌통을 선택하고, 선택한 벌통에서 꿀을 채취할 수 있다.
두명의 일꾼이 선택한 벌통은 서로 겹치면 안된다.
"""

T = int(input()) # test case개수를 받아오는 코드
for tc in range(1, T+1):
    pass